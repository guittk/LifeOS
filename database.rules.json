{
  "rules": {
    ".read": false,
    ".write": false,

    "users": {
      "$uid": {
        ".read":  "auth != null && (auth.token.email === 'guittk@hotmail.com' || auth.token.email === 'julialealdecamargo@hotmail.com') && (auth.uid === $uid || root.child('boards').child($uid).child('members').child(auth.uid).exists())",
        ".write": "auth != null && (auth.token.email === 'guittk@hotmail.com' || auth.token.email === 'julialealdecamargo@hotmail.com') && (auth.uid === $uid || root.child('boards').child($uid).child('members').child(auth.uid).exists())",

        "Profile": {
          "Email":       { ".validate": "newData.isString() && newData.val().length <= 320" },
          "DisplayName": { ".validate": "newData.isString() && newData.val().length <= 80" }
        }
      }
    },

    "boards": {
      "$boardId": {
        ".read": "auth != null && (auth.token.email === 'guittk@hotmail.com' || auth.token.email === 'julialealdecamargo@hotmail.com') && (auth.uid === $boardId || data.child('members').child(auth.uid).exists())",

        "meta": {
          ".write": "auth != null && auth.uid === $boardId",
          "ownerUid":  { ".validate": "newData.val() === $boardId" },
          "name":      { ".validate": "newData.isString() && newData.val().length <= 120" }
        },

        "members": {
          ".write": "auth != null && auth.uid === $boardId",
          "$memberUid": {
            ".write": "auth != null && $memberUid === auth.uid && root.child('boardInvites').child(auth.token.email.replace('.', '_')).child($boardId).exists()",
            "email":       { ".validate": "newData.isString()" },
            "role":        { ".validate": "newData.val() === 'owner' || newData.val() === 'member'" },
            "permissions": { "$view": { ".validate": "newData.isBoolean()" } }
          }
        }
      }
    },

    "boardInvites": {
      "$emailKey": {
        ".read":  "auth != null && (auth.token.email === 'guittk@hotmail.com' || auth.token.email === 'julialealdecamargo@hotmail.com')",
        ".write": "auth != null && (auth.token.email === 'guittk@hotmail.com' || auth.token.email === 'julialealdecamargo@hotmail.com')"
      }
    },

    "openAiKey": {
      ".read":  false,
      ".write": false
    }
  }
}
